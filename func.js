const main = require('./stama.js');
//var prefix = require('./info.js').prefixo;





function startsWith(msg)
{
	var pre = msg.split('',1);
	if(pre == prefix)
		return true;
	else
		return false;
}
